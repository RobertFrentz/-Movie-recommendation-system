@page "/moviesdata"
@inject HttpClient Http

<h3>MoviesData</h3>

@if (movies == null)
{
    <p><em>Loading Movies Data...</em></p>
}
else
{
    @*<table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>FirstName</th>
                <th>Age</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var movie in movies)
            {
                <tr>
                    <td>@movie.Id</td>
                    <td>@movie.Title</td>
                    <td>@movie.Genres</td>
                    <td>@movie.PosterUrl</td>
                </tr>
            }
        </tbody>
    </table>*@

    <div class="container">

        <h1 class="font-weight-light text-center text-lg-left mt-4 mb-0">Recommended for You</h1>

        <hr class="mt-2 mb-5">


        <div class="row text-center">
            @foreach (var item in movies)
            {
                <div class="col-lg-3 col-md-6 col-12" style="width: 300px; height: 450px; ">
                    <a href="#">
                        <img class="img-responsive" src="@item.PosterUrl" alt="" style="width:100%; height:400px">
                        <p class="text-center">@item.Title</p>
                    </a>
                </div>

            }
        </div>

    </div>

}

@code {
    private MovieDetails[] movies;

    private string apiUrl = "http://localhost:5000/api/Movies/MoviesDetails";

    protected override async Task OnInitializedAsync()
    {
        Console.WriteLine("Intentionez sa fac ceva");
        movies = await Http.GetFromJsonAsync<MovieDetails[]>(apiUrl);
        Console.WriteLine("Am facut ceva");
    }

    public class MovieDetails
    {
        public int Id { get; set; }
        public string Title { get; set; }
        public string Genres { get; set; }
        public string PosterUrl { get; set; }
    }
}
